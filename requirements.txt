# PhysicsAgent — dependencies
# Python 3.11 required (see .python-version)
#
# PINNING STRATEGY:
#   Only packages we directly import are pinned.
#   pydantic and pydantic-core are intentionally unpinned —
#   pydantic 2.11.4 has no pre-built wheel for Python 3.14,
#   causing Rust compilation failures on Render. Letting pip
#   resolve the version ensures a pre-built wheel is used.

# Core LLM & agent framework
langchain==0.3.25
langchain-openai==0.3.18
langgraph==0.4.5

# arXiv paper retrieval
arxiv==2.2.0

# API layer
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic>=2.7.0,<3.0.0

# Environment variable management
python-dotenv==1.1.0

# HTTP client (used internally by openai sdk)
httpx==0.28.1